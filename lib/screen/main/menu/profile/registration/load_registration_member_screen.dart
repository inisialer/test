import 'package:flutter/material.dart';

class LoadRegistrationMemberScreen extends StatelessWidget {
  const LoadRegistrationMemberScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ListView(
      children: [],
    );
  }
}
